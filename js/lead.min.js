window.addEventListener("DOMContentLoaded", function () {
  const track = document.querySelector("input[name='track']");
  if (track) {
    track.value = new URLSearchParams(window.location.search).get("track") || "";
  }
});
